# l3mcore light — Dependencias mínimas
# Solo lo necesario para el router + modelos locales + Ollama

# Servidor HTTP
flask>=3.0.0
gunicorn>=22.0.0

# Router semántico (embedding mode)
sentence-transformers>=2.2.2
transformers>=4.30.0
torch                 # CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu
sentencepiece>=0.1.99

# Keyword fallback (fuzzy)
rapidfuzz>=3.0

# Modelos ONNX locales
onnxruntime>=1.15.0
numpy>=1.24.0

# Motor GGUF fallback
llama-cpp-python>=0.2.0
